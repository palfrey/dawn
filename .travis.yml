language: rust
rust:
  - stable
  - beta
  - nightly
cache: cargo

matrix:
  allow_failures:
    - rust: nightly
  fast_finish: true

script:
- travis-cargo build -- $FEATURES
- travis-cargo test -- $FEATURES

env:
  matrix:
  - FEATURES=--features=lambda
  - FEATURES=
